'use strict';

// Standalone/header calculator removed intentionally.
// The only calculator kept in Diagnostika is the one inside the currency converter.
window.__diagnostikaStandaloneCalculatorReady = true;
